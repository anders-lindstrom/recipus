/**
 * Seed data for a fresh database: the category walking order and the catalog
 * vocabulary a household taps to build a shopping list. See seed-data.test.ts
 * for the invariants this data must hold.
 */

export { CATEGORIES, type SeedCategory } from "./categories";
export { CATALOG_ITEMS, type SeedItem } from "./catalog";
